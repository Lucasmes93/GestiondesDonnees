<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestiondesdonnees">

    <!-- Autorisations requises pour l'application -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="GestionDesDonnees"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">

        <!-- Activité principale -->
        <activity android:name=".viewmodel.MainActivity"
            android:exported="true">
            <intent-filter>
                <!-- Indique que cette activité est le point d'entrée -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
